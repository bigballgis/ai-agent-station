port 7001
cluster-enabled yes
cluster-config-file nodes.conf
cluster-node-timeout 5000
appendonly yes
bind 0.0.0.0
protected-mode no

# 密码认证配置（通过 docker-compose.yml 的 command 参数传递 --requirepass）
# requirepass ${REDIS_PASSWORD}
# masterauth ${REDIS_PASSWORD}
