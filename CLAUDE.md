# Claude / AI Agent Entry

This repository uses `AGENTS.md` as the single source of truth for AI coding agents.

Before making changes:

1. Read `AGENTS.md`.
2. Read only the task-relevant documents linked from `AGENTS.md`.
3. Use the project Skills in `.cursor/skills/` when the task matches them.
4. Keep changes small, reversible, and verified.

Do not use old Round-based progress logs as implementation truth. If a document conflicts with code or `AGENTS.md`, trust code first, then `AGENTS.md`, then current docs.

