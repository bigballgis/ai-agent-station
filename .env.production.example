# ============================================================
# SECURITY WARNING: DO NOT USE THESE VALUES IN PRODUCTION!
# ============================================================
# This file is a template only. All values below are INSECURE defaults.
# You MUST change ALL passwords, secrets, and keys before deploying.
# ============================================================

# AI Agent Station - Production Environment
SPRING_PROFILES_ACTIVE=production

# Database
DB_HOST=prod-db.internal
DB_PORT=5432
DB_NAME=aiagent_production
DB_USERNAME=aiagent
DB_PASSWORD=changeme

# Redis
REDIS_HOST=prod-redis.internal
REDIS_PORT=6379
REDIS_PASSWORD=changeme

# JWT
JWT_SECRET=changeme-jwt-secret-production
JWT_EXPIRATION=3600

# Server
SERVER_PORT=8080

# Logging
LOGGING_LEVEL=WARN
